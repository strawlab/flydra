function(keys,values,rereduce) {

    // This is structured something like the reduce of tag-cloud in
    // taskr, because I could get the evently stuff to work then. It
    // probably wouldn't be necessary if I could figure this out.

    return 1;
}
